#include "main.h"
#include <string.h>
/**
 * _strlen - unction that returns the length of a string
 *@s:string pointer
 * Return: Always 0
 */
int _strlen(char *s)
{
	int count = 0;

	while (*(s + count) != '\0')
		count++;
	return (ount);
}
