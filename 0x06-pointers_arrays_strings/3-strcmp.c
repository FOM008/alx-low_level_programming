#include "main.h"
#include <string.h>
/**
 * _strcmp -  compares two strings
 * @s1: input 1
 * @s2: input 2
 * Return: Always 0
 */
int _strcmp(char *s1, char *s2)
{
	int comp;

	comp = strcmp(s1, s2);
	return (comp);
}
